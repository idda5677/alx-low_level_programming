/**
 * _abs - Computes the absolute value of an integer.
 * @x: The integer for which absolute value is to be computed.
 *
 * Return: The absolute value of x.
 */
int _abs(int x)
{
    if (x < 0)
    {
        return -x;
    }
    else
    {
        return x;
    }
}

